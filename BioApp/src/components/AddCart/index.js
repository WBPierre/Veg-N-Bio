// @Flow

import React from 'react';
import { View, Text, StyleSheet } from 'react-native'
import config from '../../modules/config'


type PropsType = {

};

const AddCart = (props: PropsType) => {

  const { text, color, onPress, style } = this.props;

  return (
    <View>
      <Text>SAut</Text>
    </View>
  );
}

export default AddCart;